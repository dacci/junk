version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - ./gradlew build

artifacts:
  files:
    - '*'
  base-directory: build/distributions

cache:
  paths:
    - '/root/.gradle/**/*'
